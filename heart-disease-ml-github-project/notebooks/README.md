# Notebooks

The project is implemented as reproducible Python scripts in `src/`.

If a notebook is required for demonstration, the same workflow can be opened in Jupyter using the functions in `src/common.py`. Keeping the core implementation in `.py` files makes the project easier to version, test and reuse.
