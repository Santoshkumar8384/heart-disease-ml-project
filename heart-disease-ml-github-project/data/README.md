# Data

The project uses the public UCI Heart Disease dataset (Dataset ID 45).

The dataset is intentionally not committed here. `src/common.py` downloads it through the `ucimlrepo` package when the project is executed.

This avoids committing a third-party dataset copy and keeps the repository lightweight.
